name: Testing

on:
  pull_request:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  unit-testing:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: npm install
      - run: npm i
